VITE_API_BASE_URL=http://localhost:8000/api/v1
VITE_COMFYUI_EMBED_URL=http://34.42.194.164:8189

# Investor demo mode: hide sidebar/header, only show formal-backfill page.
# Default false. Can also be toggled at runtime via URL ?investor=1 / ?investor=0
# (which persists in localStorage).
VITE_INVESTOR_MODE=false
